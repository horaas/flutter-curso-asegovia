import 'package:flutter/material.dart';

class GiroscopeBallScreen extends StatelessWidget {

  const GiroscopeBallScreen({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}