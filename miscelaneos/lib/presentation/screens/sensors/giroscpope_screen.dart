import 'package:flutter/material.dart';

class GiroscpopeScreen extends StatelessWidget {

  const GiroscpopeScreen({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}