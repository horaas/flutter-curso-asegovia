import 'package:flutter/material.dart';

class AnimatedScreenDart extends StatelessWidget {

  const AnimatedScreenDart({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}